<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="/my_styles.css">

  </head>

  <body class="mx-3">

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary px-0">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">FindMyFurniture</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarText">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/">My Houses</a>
            </li>
            <% if current_user != nil %>
              <li class="nav-item">
                <a class="nav-link" href="users/sign_out">Sign Out</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="users/edit">Edit Profile</a>
              </li>

            <% else %>
              <li class="nav-item">
                <a class="nav-link" href="users/sign_up">Sign Up</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="users/sign_in">Sign In</a>
              </li>

            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <hr>

    <% if notice.present? %>
      <div style="color: green">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div style="color: red">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>

  </body>
</html>
